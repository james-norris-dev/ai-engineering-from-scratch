# Lesson 2: Git & Collaboration — Notes

Completed the daily git workflow exercise: created this file, committed it,
and pushed it to `my-progress` on my fork.

Commands used:
- `git add`
- `git commit -m "..."`
- `git push origin my-progress`
